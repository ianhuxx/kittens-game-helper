{
  "description": "More hands-off profile for an existing Kittens Game save. Enables safe automation and time acceleration, but still leaves prestige/reset automation disabled until you opt in after exporting a backup.",
  "settings": {
    "engine": {
      "enabled": true,
      "interval": 1000,
      "ksColumn": { "enabled": true },
      "highlighStock": { "enabled": false }
    },
    "timeControl": {
      "enabled": true,
      "accelerateTime": { "enabled": true, "trigger": 1 },
      "timeSkip": {
        "enabled": true,
        "trigger": 5,
        "max": 0,
        "ignoreOverheat": { "enabled": false }
      },
      "reset": { "enabled": false }
    }
  }
}
