import type{ScoreInput}from'../types';
export function scoreEvent(e:ScoreInput):number{let s=20;const f=e.formType.toUpperCase();if(f==='SC 13D'&&e.event_type==='new_stake')s+=25;if(f.includes('13D/A'))s+=15;if(f.includes('13D/A')&&e.event_type==='increase')s+=15;const p=e.percentOwned??0;if(p>=15)s+=20;else if(p>=10)s+=15;else if(p>=5)s+=5;if(['tender','liquidation','open_end'].includes(e.campaign_type))s+=20;if(e.campaign_type==='board'||e.event_type==='proxy')s+=20;if(e.campaign_type==='settlement')s+=10;if(e.campaign_type==='discount')s+=10;if(['saba','bulldog'].includes(e.activistSlug||''))s+=5;if(f.includes('13G')&&e.campaign_type==='passive')s-=10;if(f.includes('13F'))s-=20;if(e.event_type==='decrease')s-=10;if(e.hasIssuer===false)s-=10;return Math.max(0,Math.min(100,s))}
export function priorityLabel(s:number){return s>=80?'Must review today':s>=60?'High-priority idea':s>=40?'Add to watchlist':s>=20?'Low-priority filing':'Archive/reference only'}
