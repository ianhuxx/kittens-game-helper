import React from'react';import{StatusBadge}from'./StatusBadge';export function EventCard({event:e}:{event:any}){return <article className="card"><div><b>{e.activist_name}</b> · {e.issuer_name||'Unknown issuer'} {e.ticker&&`(${e.ticker})`}</div><h3>{e.summary}</h3><p>{e.form_type} · {e.filing_date} · {e.event_type} · {e.campaign_type}</p><p>Ownership: {e.percent_owned??'—'}% · Score <b>{e.score}</b></p><StatusBadge status={e.status}/><p>{e.item4_text?.slice(0,220)}</p><a href={e.sec_url} target="_blank">Open SEC filing</a> <button>Read Item 4</button> <button>Add to watchlist</button> <button>Mark reviewed</button></article>}
